<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>

  <header>
    <% include partials/_header %>
  </header>

   <h2>URLs and Shortened Forms</h2>

  <% if (locals.user) { %>
     <ul>
    <% for(var key in urls) { %>

      <form method="POST" action="/urls/<%= key %>/delete">
         <li>
          <%= key %> -> <a href="<%=urls[key].longURL%>"> <%= urls[key].longURL %></a> <a href="<%=`http://localhost:8080/urls/${key}`%>" >Edit</a> <button type ="delete" value="delete">Delete</button>
        </li>

      </form>

     <% } %>
     <% } %>
     <br>
     <a href="/urls/new">Click here to shorten a new URL</a>

  </ul>

</body>
</htmll>



